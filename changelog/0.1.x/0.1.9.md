---
summary: "Adopts @cyanheads/mcp-ts-core ^0.13.6 — a rejected tool call now names the reason and carries a schema-derived recovery hint, tool error text states the reason and retryability, unknown and misspelled argument keys are repaired before a call fails, and a mid-flight cancellation classifies RequestCancelled."
breaking: false
security: false
---

# 0.1.9 — 2026-09-20

## Changed

- **An argument rejection carries `data.reason: "invalid_arguments"` and a schema-derived recovery hint**, rendered as a `Recovery:` line in `content[]`. A required field omitted from an enum now renders as missing rather than as a wrong choice, and a rejection against alternative shapes renders each branch's own message instead of a bare `Invalid input`.
- **Tool error text closes with `(reason … · not retryable)`** whenever the thrown error's data carries a `reason` or a boolean `retryable`.
- **Tool arguments pass a pre-validation step before schema parsing** — a client-added root key is dropped, a case-style key alias is rewritten, and a JSON-stringified array argument is repaired once before a call fails.
- **A request cancelled mid-flight classifies `RequestCancelled` (-32011)** instead of a generic internal error.
- **`createApp()` declares `sessionMode: 'stateless'` in code**, and each service-thrown `not_found` / `upstream_error` / `pagination_ceiling` reason carries `thrownBy: 'service'`.
- Skill tree moved `skills/` → `framework-skills/` (dev tooling; not part of the published package).

## Dependencies

- `@cyanheads/mcp-ts-core` `^0.12.3` → `^0.13.6`
- `zod` `^4.4.3` → `^4.6.5`
- `@biomejs/biome` `^2.5.10` → `^2.5.14` (dev)
- `@types/node` `^26.2.0` → `^26.6.1` (dev)
- `ignore` `^7.0.6` → `^7.0.9` (dev)
- `tsc-alias` `^1.9.2` → `^1.9.5` (dev)
- `vitest` `^4.1.11` → `^5.0.1` (dev, major)
- `engines.bun` `>=1.3.0` → `>=1.4.0`
