---
summary: "Public hosted endpoint at https://nonprofit-explorer.caseyjhand.com/mcp — server.json remotes, README hosted block and install section."
breaking: false
security: false
---

# 0.1.3 — 2026-06-15

## Added

- **Public hosted endpoint** — server is live at `https://nonprofit-explorer.caseyjhand.com/mcp` (Streamable HTTP).
- **`server.json` `remotes`** — top-level `remotes` array added with the hosted Streamable HTTP URL, enabling MCP Registry clients to discover and connect without local installation.
- **README hosted server block** — centered `Public Hosted Server` link added below the badge row.
- **README `### Public Hosted Instance`** — new subsection at the top of "Getting started" with the Streamable HTTP client config; no installation required.

## Dependencies

- `vitest` ^4.1.8 → ^4.1.9
